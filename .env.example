# ============================================================
# Bracket of Death - Environment Configuration
# ============================================================
# For local development/testing: All values have sensible defaults.
# For production: Change the REQUIRED SECRETS section below.
# ============================================================

# ============================================================
# REQUIRED FOR PRODUCTION (must change from defaults)
# ============================================================
# These have insecure defaults - CHANGE THESE for any real deployment!

# MongoDB password (default: bodpassword123)
# MONGO_PASSWORD=your_secure_mongo_password

# Keycloak admin password (default: keycloak123)
# KEYCLOAK_ADMIN_PASSWORD=your_secure_keycloak_admin_password

# Keycloak database password (default: keycloak123)
# KEYCLOAK_DB_PASSWORD=your_secure_keycloak_db_password

# Keycloak OAuth client secret (default: bodclient123)
# KEYCLOAK_CLIENT_SECRET=your_secure_client_secret

# JWT signing secret (default: bracket-of-death-jwt-secret-key-2024)
# JWT_SECRET=your_secure_jwt_secret_at_least_32_chars

# ============================================================
# DEPLOYMENT CONFIGURATION
# ============================================================
# Set these when deploying to a server or custom domain

# Your application's public URL (default: http://localhost:5173)
# APP_URL=https://yourdomain.com

# Frontend port to expose (default: 5173)
# PORT=5173

# CORS allowed origins - comma-separated (default: http://localhost:5173,http://127.0.0.1:5173)
# CORS_ORIGIN=https://yourdomain.com

# ============================================================
# OPTIONAL - INTERNAL DEFAULTS (rarely need to change)
# ============================================================
# These have sensible defaults and work out of the box

# Node environment (default: production for ghcr, development for local)
# NODE_ENV=production

# Keycloak admin username (default: admin)
# KEYCLOAK_ADMIN_USER=admin

# Keycloak realm name (default: bracketofdeathsite)
# KEYCLOAK_REALM=bracketofdeathsite

# Keycloak client ID (default: bod-app)
# KEYCLOAK_CLIENT_ID=bod-app

# Frontend proxy paths (default: /api and /auth)
# VITE_API_URL=/api
# VITE_KEYCLOAK_URL=/auth

# ============================================================
# EMAIL CONFIGURATION (Optional - can configure in admin UI)
# ============================================================
# Mailjet credentials for sending emails (invites, password resets)
# You can also configure this through the admin dashboard

# MAILJET_API_KEY=your_mailjet_api_key
# MAILJET_API_SECRET=your_mailjet_api_secret
# MAILJET_SENDER_EMAIL=noreply@yourdomain.com

# ============================================================
# STRIPE CONFIGURATION (Required for paid tournaments)
# ============================================================
# Get these from https://dashboard.stripe.com/apikeys

# STRIPE_PUBLISHABLE_KEY=pk_test_your_key_here
# STRIPE_SECRET_KEY=sk_test_your_key_here
# STRIPE_WEBHOOK_SECRET=whsec_your_secret_here

# ============================================================
# MAILGUN CONFIGURATION (Alternative email provider)
# ============================================================
# Configure via admin UI or set here

# MAILGUN_API_KEY=your_mailgun_api_key
# MAILGUN_DOMAIN=mg.yourdomain.com